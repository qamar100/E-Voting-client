import React from 'react'

interface ElectionFormDialogProps {
    children: React.ReactNode
}
export const ElectionForm = ({children}: ElectionFormDialogProps) => {
  return (
    <div>ElectionForm</div>
  )
}